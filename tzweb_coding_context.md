# APP 体征分析后台项目上下文压缩版

> 用途：新开一个 coding 窗口时，把这份文档作为项目背景交给 Codex / Claude Code。  
> 当前项目目录：`/Users/admin/Documents/tzweb`  
> 当前阶段目标：先完成“评论下载 + 本地留痕 + 后台查看记录”，暂不做 AI 解析。后续版本再做评论解析、标签聚类、体征分析。

---

## 1. 项目目标

我要做一个 **APP 体征分析后台**。

第一版只做：

1. 用户输入目标 App 的商店地址。
2. 后台调用 Rivioo 的能力下载最新评论。
3. 评论文件保存到本地。
4. 网页后台能查看下载任务记录。
5. 本地下载记录中可以：
   - 下载原始文件。
   - 查看/打开本地原文件位置。
6. 暂时不做评论解析、AI 打标签、聚类分析。

后续版本再做：

- 选择评论解析模板。
- 调用 DeepSeek 并行解析评论。
- 给评论打标签。
- 不符合模板的内容单独输出，供用户确认标签归类。
- 展示评论分析结果、竞品问题、用户需求、负评原因、版本问题等。

---

## 2. 关键产品判断

目前不是要自建稳定的数据采集 SaaS，也不是直接接官方 API。

当前明确要求：

> 直接使用 Rivioo 的能力，能下载评论到本地就行。

Rivioo 地址：

```text
https://www.rivioo.app/
```

但注意：

- 不要把 Rivioo 后端接口写死。
- 启动时要自动从 Rivioo 首页解析当前后端接口。
- 如果 Rivioo 后续改接口，项目需要跟着改。
- 当前版本接受这种不稳定性，因为目标是先快速完成本地下载能力。

---

## 3. 技术方案现状

项目是一个低成本本地后台，没有引入 Next.js / 数据库。

当前技术方案：

- Node.js 原生 HTTP 服务。
- 前端是静态页面：
  - `public/index.html`
  - `public/app.js`
  - `public/styles.css`
- 后端入口：
  - `server.mjs`
- Rivioo 逻辑：
  - `src/rivioo-client.mjs`
- 本地任务存储：
  - `src/local-store.mjs`
- CLI 下载脚本：
  - `scripts/rivioo-download.mjs`
- 本地数据目录大致为：
  - `data/reviews/`
  - `data/reviews/files/`
- 早期输出目录曾用过：
  - `outputs/reviews/`

---

## 4. 已有文件

项目已有这些核心文件：

```text
package.json
README.md
.gitignore
server.mjs
src/rivioo-client.mjs
src/local-store.mjs
scripts/rivioo-download.mjs
public/index.html
public/app.js
public/styles.css
```

---

## 5. 运行方式

本地后台运行：

```bash
npm run dev
```

默认尝试端口：

```text
http://localhost:3000
```

如果 3000 被占用，服务会自动尝试后续端口，例如：

```text
http://localhost:3001
```

注意：

- 不要直接打开 `file:///Users/admin/Documents/tzweb/public/index.html`
- 必须通过 `http://localhost:端口` 打开
- 因为 `file://` 不能正常访问本地后端 API

---

## 6. 已经验证过的能力

之前已经验证过：

1. CLI 方式可以用 Rivioo 下载评论。
2. 用 WhatsApp Google Play 链接下载过 10 条评论。
3. CSV 成功落地，本地文件 11 行，包含表头和 10 条评论。
4. App Store URL 解析和导出链路也测过。
5. Rivioo / App Store 当前最多 500 条，脚本会把 iOS 的 `--count 1000` 自动降到 500。
6. 本地后台接口能返回：
   - `/`
   - `/api/config`
   - `/api/jobs`
7. `node --check` 多次通过：
   - `server.mjs`
   - `src/rivioo-client.mjs`
   - `src/local-store.mjs`
   - `scripts/rivioo-download.mjs`
   - `public/app.js`

---

## 7. 绝对硬性要求

### 7.1 所有代码必须有中文注释

从现在以及以后所有开发，都必须给代码加清晰、易懂的中文注释。

注释要求：

- 让产品经理能看懂。
- 不要只是逐行翻译语法。
- 要解释这段代码在业务上做什么。
- 尤其是接口、状态流转、文件保存、任务轮询、错误处理，必须写清楚。

已经要求 Codex 把现有代码“洗”过一遍中文注释，后续新增代码也必须延续这个要求。

### 7.2 网页后台必须中文

所有可见文案都要中文。

包括：

- 导航
- 标题
- 表单
- 按钮
- 状态
- 错误提示
- 空状态
- 表格字段
- 任务记录
- 下载/查看按钮

### 7.3 界面不要太丑

之前已经要求使用新装的 frontend-design skill 重新设计。

Codex 找到过：

```text
/Users/admin/.codex/skills/frontend-design
```

设计方向已经改过：

- 不要普通白卡片后台。
- 要更像“评论数据战情室 / 中文编辑部报表感”。
- 深色侧栏。
- 内容区有报表感。
- 保持后台工具的密度和效率。
- 但不能堆无意义模块。

如果后续继续改 UI，优先沿用这个方向。

---

## 8. 页面结构修改历史

### 8.1 已经删除的内容

用户明确要求删除，并且已经处理过：

删除顶部大标题说明：

```text
采集工作台
下载目标 APP 的最新评论
把目标商店链接变成本地原始评论资产。当前阶段只负责采集和留痕，后续体征分析直接基于这些文件展开。
```

删除能力条：

```text
数据源
Rivioo 导出接口
02
本地存储
data/reviews
03
下一阶段
评论解析与主题归因
```

删除提示文案：

```text
建议先按国家和语言分别下载，后续分析口径会更清楚。
```

删除“新建下载任务”右侧的状态胶囊，例如：

```text
已完成
```

把：

```text
Rivioo 限制
```

改成：

```text
导出限制
```

右上角独立接口模块也已经删除，不要再单独放：

```text
当前 Rivioo 接口
https://rivioo-api.up.railway.app/api
```

接口状态要简单合并到“新建下载任务”模块下方，不要单独占一个显眼模块。

### 8.2 顶部状态

用户后来觉得顶部状态“感觉没有”，要求去掉。

因此顶部状态指标已经移除，只保留必要的接口状态信息，并且不能做成一个独立的大模块。

---

## 9. 表单字段要求

### 9.1 国家/地区

“国家/地区”不能是手输，必须是下拉选项。

选项来源：

- 从 Rivioo 当前页面提取。
- 不要自己拍脑袋写一批。

### 9.2 评论数量

“评论数量”不能是手输，必须是下拉选项。

Rivioo 当前数量档位曾提取到：

```text
500
1000
2000
5000
10000
```

### 9.3 语言

语言选项要用 Rivioo 的。

之前已从 Rivioo 当前页面整理过：

```text
全部语言
英语
西班牙语
法语
德语
```

如果继续完善，优先从 Rivioo 页面动态/半动态同步，不要自造过多语言。

---

## 10. 本地下载记录功能

本地下载记录需要展示任务记录和文件操作。

每条记录至少应该包含：

- 平台
- App 链接或 App ID
- 国家/地区
- 语言
- 评论数量
- 文件格式
- 任务状态
- 创建时间 / 完成时间
- 文件名
- 操作按钮

操作按钮：

1. 下载
2. 查看原文件 / 打开原文件位置

---

## 11. “查看原文件”的真实需求

这里之前发生过多次误解，必须记住最终语义。

### 11.1 错误理解 1：浏览器内预览 CSV

之前实现过：

```text
/api/preview/{任务ID}?filename=xxx.csv
```

返回 HTML 预览页。

用户说：

> 错了，没有打开本地目录，而是在浏览器开了个窗口

所以这个不是用户要的。

### 11.2 错误理解 2：浏览器打开 CSV

之前实现过：

```text
.csv?view=1
```

或者 inline content-disposition。

但浏览器还是可能下载，用户不满意。

### 11.3 最终正确理解

“查看原文件”应该是：

> 打开本机保存位置，在 Finder 中定位到对应 CSV/XLSX 文件。

也就是 macOS 上由 Node 后端执行类似：

```bash
open -R "/path/to/file.csv"
```

要求：

- 前端按钮点击后，不跳转新网页。
- 调用本地后端接口。
- 后端在 Finder 中选中本地文件。
- 成功后页面可以轻量提示“已在 Finder 中定位文件”。
- 失败时返回中文错误，例如“无法打开本地文件位置，请确认文件仍存在”。

---

## 12. 当前最新未完成 / 正在处理的问题

最后一次对话进度停在这里：

用户指出：

> 错了，没有打开本地目录，而是在浏览器开了个窗口

Codex 回应：

- 已理解“查看原文件”应为 Finder 定位。
- 会保留“下载”链接。
- 把“查看原文件”改成按钮。
- 点击后请求本地服务执行 `open -R 文件路径`。
- 这个能力只在本地服务运行时有效。

Codex 已做过：

```text
已探索 3 个文件,1 次搜索
已编辑 3 个文件
```

但最后还没看到完整验证结果，只说：

> 按钮逻辑已经改成 Finder 定位。现在我做语法检查、接口检查，再重启服务验证 /api/reveal/... 能返回成功。

所以新 coding 窗口接手时，第一件事应该是：

1. 检查最近修改的这 3 个文件。
2. 确认是否已经新增 `/api/reveal/...` 接口。
3. 确认前端“查看原文件”按钮是否调用该接口，而不是 `<a target="_blank">` 打开网页。
4. 确认后端是否用 macOS `open -R` 打开 Finder。
5. 跑语法检查。
6. 启动服务。
7. 点按钮实测 Finder 是否打开并选中文件。

---

## 13. 建议的新窗口第一条指令

可以把下面这段直接发给 Codex / Claude Code：

```text
请阅读 /Users/admin/Documents/tzweb 项目，并继续完成“查看原文件”功能。

当前需求：
1. “下载”按钮继续保持下载 CSV/XLSX 文件。
2. “查看原文件”不是浏览器预览，也不是下载，而是打开本地 Finder，并选中该任务对应的原始评论文件。
3. 前端按钮点击后不要跳新窗口，应该调用后端接口。
4. 后端在 macOS 下使用 open -R 定位文件。
5. 如果文件不存在或打开失败，返回中文错误提示。
6. 所有新增代码必须有清晰中文注释，让产品经理能看懂。
7. 修完后请验证：
   - node --check server.mjs
   - node --check public/app.js
   - 启动 npm run dev
   - 调用 /api/reveal/... 接口返回成功
   - 页面点击“查看原文件”可以在 Finder 中定位到文件
8. 不要恢复之前已经删除的页面模块和文案。
```

---

## 14. 重要不要做

不要做这些事：

1. 不要把 Rivioo API 写死。
2. 不要把“查看原文件”做成浏览器预览。
3. 不要让“查看原文件”触发下载。
4. 不要恢复顶部大标题说明。
5. 不要恢复“数据源 / 本地存储 / 下一阶段”能力条。
6. 不要恢复右上角单独的“当前 Rivioo 接口”模块。
7. 不要恢复“建议先按国家和语言分别下载...”这类提示。
8. 不要把国家/评论数量/语言改回手输。
9. 不要写英文后台文案。
10. 不要写没有中文注释的新代码。
11. 当前版本不要做 AI 评论解析，不要过早接 DeepSeek。

---

## 15. 后续版本方向，但当前先不做

后续可以做的分析能力：

- 评论模板编辑。
- 评论解析模板。
- DeepSeek 20 并行解析。
- 每条评论多标签。
- 不符合模板的内容输出到单独列。
- 人工确认新标签归类。
- 评论主题聚类。
- 负评原因分析。
- 版本问题分析。
- 竞品机会点分析。
- 国家/语言/版本维度对比。
- 最近 7 天 / 30 天新增高频投诉。
- 评分趋势。
- 负评占比。
- 高频关键词。
- bug / 功能请求 / 体验问题 / 价格问题分类。

但当前版本只做采集下载和本地留痕。

---

## 16. 当前项目的产品定位一句话

这是一个本地运行的 APP 评论采集后台，第一版通过 Rivioo 能力把目标 App 的评论下载到本地，并沉淀成后续 AI 体征分析的数据资产。
